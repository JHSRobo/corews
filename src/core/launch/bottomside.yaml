launch:

# Arguments for the various nodes.
# These can be set from cmd line if necessary

# Node for gpio_control
- node:
    pkg: "gpio_interface"
    exec: "gpio_control"
    name: "gpio_interface"